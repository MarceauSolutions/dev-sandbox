Pre-load context for a project or topic.

Run: `python execution/context_preloader.py --project $ARGUMENTS`

Options:
- `--project <name>` — Load context for a specific project
- `--auto` — Auto-detect from recent git changes
- `--deep` — Print file contents, not just paths

Use this at the start of a session or when switching between projects.
